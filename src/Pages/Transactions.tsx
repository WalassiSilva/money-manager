import { TransactionsList } from "../components/Transactions/TransactionsList";

const Transactions = () => {
  return (
    <div className="page-fade-in">
      <TransactionsList />
    </div>
  );
};

export { Transactions };
